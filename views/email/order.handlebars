<html>
<head>
</head>
<body style="margin:10px">

<div><a href="{{link}}">查詢訂單 / 通知轉帳</a></div>

<div>
    <h2 style="font-size:17px">轉帳帳號</h2>
    <ul style="padding:10px; line-height: 25px; border: 1px solid #ccc; border-radius: 5px; list-style:none;">
        {{#brand}}
        <li><span>銀行代碼：</span><span>{{bankCode}}</span></li>
        <li><span>帳號：</span><span>{{bankAccountNumber}}</span></li>
        <li><span>戶名：</span><span>{{bankAccountName}}</span></li>
        {{/brand}}
        <li><span>金額：</span><span>{{subtotal}}</span></li>
    </ul>
</div>

<table style="width:100%; margin-top:20px; line-height:30px; font-size:15px">

    {{#each order.items}}
    <tr>
        <td>{{{productName}}}</td>
        <td style="text-align:right"><span>NT${{price}}</span> <span>x</span> <span>{{count}}</span></td>
    </tr>
    {{/each}}
    <tr>
        <td>運費</td>
        <td style="text-align:right">NT$ {{order.shipping}}</td>
    </tr>
    <tr>
        <td>加總</td>
        <td style="text-align:right">NT$ {{subtotal}}</td>
    </tr>
</table>

{{#if order.note}}
<p>備註：{{order.note}}</p>
{{/if}}
</body>
</html>
